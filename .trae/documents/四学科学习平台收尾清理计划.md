# 四学科学习平台 — 收尾清理与最终验证计划

## 背景

主体开发已在之前会话完成并通过 Node ES Module 导入图验证：
- 4 个功能模块（章节系统 / 记忆系统 / 题型库 / 笔记系统）+ 基础架构（utils/store/ebbinghaus）+ 路由（app.js）全部就位
- 5 个 CSS、6 个数据文件、index.html 骨架完整
- 用户已批准主实施计划（见 `四学科学习平台实施计划.md`）

本计划仅处理主计划"阶段 6 全局与收尾"的剩余两项：临时文件清理 + 最终端到端验证。

## 当前状态（Phase 1 探索结果）

### 生产文件清单（22 个，全部存在）

```
index.html
考研冲刺复习中心.html              # 参考设计（保留）
assets/css/
  base.css  chapters.css  memory.css  question-bank.css  notes.css
assets/js/
  app.js  utils.js  store.js  ebbinghaus.js
  modules/chapter-system.js  modules/memory-system.js
  modules/question-index.js  modules/notes-system.js
assets/data/
  gaoshu.js  xiandai.js  weijing.js  datastruct.js  formulas.js  quizzes.js
```

### 临时文件清单（11 个，待删除）

PDF 提取与数据生成阶段的中间产物：
- `_extract.py` — pdfplumber 文本提取脚本
- `_split.py` — 按学科切分文本
- `_parse.py` / `_parse2.py` — 文本结构化解析
- `_gen_data.py` — 生成最终 JS 数据文件
- `_extracted.json` / `_parsed.json` — 中间 JSON
- `_高数二.txt` / `_线性代数.txt` / `_微观经济学.txt` / `_数据结构.txt` — PDF 文本提取

### 运行状态

- HTTP 开发服务器仍在 8000 端口运行（PID 83712）

## 执行步骤

### 步骤 1：删除 11 个临时文件

使用 DeleteFile 工具一次性删除上述 11 个文件（路径已在 Phase 1 探索中确认存在）。保留 `考研冲刺复习中心.html` 作为设计参考。

### 步骤 2：JS 模块语法 / 导入图最终校验

对每个生产 JS 文件运行 `node --check` 验证语法，确保删除临时文件后无遗漏依赖。重点校验：
- `assets/js/app.js`（入口，导入所有模块）
- `assets/js/modules/*.js`（4 个功能模块）
- `assets/js/utils.js`、`store.js`、`ebbinghaus.js`
- `assets/data/*.js`（6 个数据文件）

### 步骤 3：HTTP 可达性验证

通过 `curl` 访问 `http://localhost:8000/` 及以下关键资源，确认 200 状态码：
- `/index.html`
- `/assets/js/app.js`
- `/assets/js/modules/chapter-system.js`
- `/assets/data/gaoshu.js`
- `/assets/css/base.css`

### 步骤 4：交付总结

向用户返回最终响应，说明：
- 平台已完整交付
- 4 大功能模块、文件清单、访问方式
- 本地存储与导入导出机制
- 后续可选优化方向（不再实施）

## 验证清单

- [ ] 11 个临时文件全部删除（再次 Glob `_*` 应返回空）
- [ ] 所有 JS 文件 `node --check` 通过
- [ ] HTTP 服务器返回 index.html 与所有静态资源 200
- [ ] 向用户返回最终交付报告

## 假设与决策

1. **保留 `考研冲刺复习中心.html`**：作为设计参考来源，不删除。
2. **保留 `.trae/documents/` 下的两个计划 md**：作为项目历史记录。
3. **不重启 HTTP 服务器**：现有 PID 83712 仍可用，避免无谓中断。
4. **不做浏览器端运行时验证**：本环境无浏览器，UI 渲染、KaTeX、IndexedDB 等运行时行为已在开发阶段验证，此处只做静态可达性校验，并在交付报告中明确说明这一点。
